
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demand.module.system.mapper.SysConfigMapper">

    <select id="selectByGroup" resultType="com.demand.module.system.entity.SysConfig">
        SELECT * FROM sys_config
        WHERE config_group = #{configGroup}
          AND is_visible = 1
        ORDER BY sort ASC
    </select>

    <select id="selectValueByKey" resultType="java.lang.String">
        SELECT config_value FROM sys_config
        WHERE config_key = #{configKey}
            LIMIT 1
    </select>

</mapper>